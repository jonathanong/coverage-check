ignoreChecks:
  # SonarCloud uses Automatic Analysis (GitHub App); sonar-project.properties
  # is not read in this mode. The quality gate threshold (≤3% duplication) is
  # strict for test code which naturally repeats fixture patterns. SonarCloud
  # is not a required branch-protection check (merge: UNSTABLE, not BLOCKED).
  - SonarCloud Code Analysis
  # Kilo Code Review stays QUEUED indefinitely and is not a required check.
  - Kilo Code Review
