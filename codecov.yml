comment: false

ignore:
  - "bin/**"
  - "src/**/*.test.mts"
  # types.mts contains only TypeScript type definitions — zero executable code
  - "src/types.mts"
  # coverage-check.mts is a barrel re-export — zero executable code
  - "src/coverage-check.mts"

coverage:
  status:
    project:
      default:
        # Preserve or improve aggregate coverage relative to the PR base.
        target: auto
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%
