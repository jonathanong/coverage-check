comment: false

ignore:
  - "src/**/*.test.mts"
  - "src/types.mts"
  - "src/coverage-check.mts"

coverage:
  status:
    project:
      default:
        target: 100%
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%
